[tool.poetry]
name = "valopy"
readme = "README.md"
packages = [{include = "valorant"}]

[tool.poetry.dependencies]
python = "^3.10"

requests = "^2.28.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
